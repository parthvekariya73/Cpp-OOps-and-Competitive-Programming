#include<bits/stdc++.h>
using namespace std;

template <class T1, class T2>
void print(unordered_map<T1,T2> &m) {
    for(auto &it : m) {
        cout << it.first << " " << it.second << endl;
    }cout << endl;
}

int main()
{
// no sorted only for un ordered element store in map
/*
    unordered_map<int, string> m;
    m[3] = "abc";
    m[1] = "def";
    m[5] = "ghi";
    m[6] = "kln";
    print(m);
*/

/*
    unordered_map<string,int> m;
    int n;
    cin >> n;
    for (int i = 0; i < n; ++i)
    {
        string s;
        cin >> s;
        m[s]++;
    }
    print(m);
*/

}